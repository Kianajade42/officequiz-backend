import React from "react";

const Result = ({score}) => (
    <div className="score-board">
        <div className="score">You answered {score} / 5 correct
        </div>
    </div>
)

export default Result;